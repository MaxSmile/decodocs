This folder contains static classification and validation specs served by the web app.

Planned:
- document-types.index.json (flat list for typeahead)
- validation/<typeId>.json (type-specific validation criteria + schemas)

These files are intended to be loaded dynamically by the UI and can be cached by the browser/CDN.
