import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';

// Placeholder tests for driveStore — marked todo/skipped until store implementation exists
describe.skip('driveStore (skeleton)', () => {
  it('stores provider tokens and refresh lifecycle (TODO)', () => {
    // TODO: add tests for token storage, refresh, revoke, and permission checks
  });

  it('exposes connect/disconnect actions and persisting metadata (TODO)', () => {
    // TODO
  });
});
