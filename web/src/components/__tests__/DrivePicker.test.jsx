import React from 'react';
import { render, screen } from '@testing-library/react';

// TODO: flesh out once DrivePicker component exists
describe.skip('DrivePicker component (skeleton)', () => {
  test('renders connect button and placeholder UI', () => {
    // render(<DrivePicker />);
    // expect(screen.getByRole('button', { name: /connect/i })).toBeInTheDocument();
  });
});
